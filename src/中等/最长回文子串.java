package 中等;

/**
 * @Description
 * @Author 脱氧核糖
 * @Version 1.0
 * @Date 2022/1/9 14:53
 */
public class 最长回文子串 {
    //
    //public String longestPalindrome(String s) {
    //    char[] str = maxString(s);
    //    int[] index = new int[];
    //    int max = 1;
    //    for (int i = 1; i < str.length; i++) {
    //        for (int j = 1; i + j < str.length && i - j >= 0; j++) {
    //            if (str[i + j] != str[j - i]) {
    //                if (max < j - i + 1) {
    //                    max = Math.max(max, j - i + 1);
    //                    index[0] =
    //                }
    //                break;
    //            }
    //        }
    //    }
    //    return
    //}
    //
    //public char[] maxString(String s) {
    //    char[] charArr = s.toCharArray();
    //    char[] res = new char[s.length() * 2 + 1];
    //    int index = 0;
    //    for (int i = 0; i != res.length; i++) {
    //        res[i] = i % 2 == 0 ? '#' : charArr[index++];
    //    }
    //    return res;
    //}
}
